这个文件夹里放得是各种基于几何表示的渲染器。
渲染器接受相机参数（以及可能的相机射线与光照等外蕴信息（不是几何表示自己的东西）），根据作为成员的几何表示进行渲染得到图像与各类深度图那样的几何。
一个渲染器针对一类接受信息与输出信息相同的几何表示。这些类的作用是尝试不同的几何表示，找到最合适的那个，想换随时可以换。
需要新几何表示，可以直接在几何表示里加个新的，看IO是不是一致，不一致了整个新的渲染器不然用原来的。
